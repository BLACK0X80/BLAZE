[package]
name = "blaze-runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
tokio = { version = "1.35", features = ["full"] }
libc = "0.2"
parking_lot = "0.12"
crossbeam = "0.8"
